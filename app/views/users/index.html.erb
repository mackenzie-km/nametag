<%= render :layout => 'layouts/card_header', :locals => {:header => "Edit User Account Access", :description => "Remember that 0 is the lowest access, and 2 is the maximum access."} do %>

  <div class="table-responsive">
    <table class="table table-hover">
        <thead class="">
          <tr>
              <th> User </th>
              <th> Admin Level </th>
              <th> Edit Access </th>
              <th> Delete User </th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td><%= user.name %> </td>
              <td><%= user.admin_level.to_s %></td>
              <td><%= button_to "Edit", edit_user_path(user), :class => "btn btn-primary", :method => :get %></td>
              <td><%= button_to "Delete", user_path(user), :class => "btn btn-primary", :method => :delete %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

<% end %>
