<h2> Edit Access for <%= "#{@user.name.capitalize}" %> </h2>

<%= form_for @user do |f| %>
  <%= f.label "Access Level:" %>
  <%= f.select(:admin_level, [0, 1, 2]) %><br>

  <%= f.submit "Submit" %><br>
<% end %>
